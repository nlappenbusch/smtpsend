services:
  smtpsend:
    build: .
    container_name: smtpsend-app
    restart: unless-stopped
    ports:
      - "8089:3000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    extra_hosts:
      - "mail.pc4play.de:192.168.50.106"
    environment:
      - NODE_ENV=production
      - PORT=3000
